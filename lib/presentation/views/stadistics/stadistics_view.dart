import 'package:flutter/material.dart';

class StadisticsView extends StatelessWidget {
  const StadisticsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}